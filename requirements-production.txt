# Production requirements for Railway - Phased approach

# Phase 1: Core dependencies (REQUIRED)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.0

# Phase 2: Web framework
aiohttp>=3.9.0
aiohttp-cors>=0.7.0
python-multipart>=0.0.6

# Phase 3: Security (REQUIRED for production)
pyjwt>=2.8.0
passlib>=1.7.4
bcrypt>=4.0.0
python-jose>=3.3.0
cryptography>=41.0.0
aiohttp-limiter>=1.1.0
slowapi>=0.1.9

# Phase 4: Core features
requests>=2.31.0
markdown>=3.5.0
beautifulsoup4>=4.12.0

# Phase 5: Optional but recommended
google-generativeai>=0.3.2  # AI features
redis>=4.5.0  # For caching (optional)
sentry-sdk>=1.40.0  # Error tracking (optional)

# Phase 6: Advanced features (add if needed)
# google-cloud-storage>=2.10.0  # GCS for attachments
# pypdf>=3.17.0  # PDF processing
# pillow>=10.0.0  # Image processing
# networkx>=3.0  # Knowledge graph
# numpy>=1.24.0  # Vector operations
# tiktoken>=0.5.0  # Token counting

# Note: mcp package excluded as it might not be on PyPI